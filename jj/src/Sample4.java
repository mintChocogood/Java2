
public class Sample4 {
	
	public static void main(String[] args) {
		// 객체 모델링 : 현실에 존재하는 객체를 유심히 관찰하고,
		//               그 객체의 속성과 행위(=기능)를 추출하는 과정
		
		boolean boolvalue = false;   // 불린 리터럴
		int age = 23;                // 정수 리터럴
		double weight = 3.24;        // 실수 리터럴
		
		// 사람의 이름같은 "문자열"이 문제
		// 문자열을 저장가능한 타입명은 "String"
		// 그런데, 이 String 타입은 기본타입에 속하지않고,
		// "참조타입" 중에 하나
		// 하지만, 데이터 중에는 문자열이 빠질 수 없기 때문에,
		// 문자열은 마치 기본타입처럼 가장 먼저 배우고 사용해야 합니다.
		
		// 리터럴: 순수한 값
		String name = "Giseo"; // 문자열 리터럴
		
		System.out.println(name);
		
		
		
	}

}
